module.exports = {
  root: true,
  // extends: '@react-native-community',
  "rules": {
    // enable additional rules
    "indent": ["error", 2],
    quotes: ["error", "single"],
    "semi": ["error", "never"],

    // disable rules from base configurations
    "no-console": "off",
  }
};
